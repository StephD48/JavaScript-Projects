
function displayType(year) { //Defining a function
    var yearType = year.getAttribute("disney-movie-year-type");//assigning a variable
    alert(yearType + " is when the Disney movie, " + year.innerHTML + " was released!");//Output

}